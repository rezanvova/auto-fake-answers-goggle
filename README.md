# Auto Answer Generator

Автоматическое заполнение Google Forms с умной генерацией ответов через AI.

## 🎯 Что это?

Система для автоматического заполнения Google Forms с использованием весов для ответов, сгенерированных AI агентом. Это позволяет создавать логичные и разнообразные ответы, имитирующие реальных респондентов.

## 🚀 Быстрая установка

### Вариант 1: Локально

```bash
# 1. Установи зависимости ( можно через uv там все готово для него, но опять же лучще через pip и не ебать себе голову)
pip install -r requirements.txt

# 2. Установи ChromeDriver
# macOS:
brew install chromedriver

# Linux:
sudo apt-get install chromium-chromedriver

# Windows:
# Скачай с https://chromedriver.chromium.org/

# 3. Настрой конфигурацию и запусти
python main.py --url "ТВОЯ_ФОРМА" -n 50 (кол-во раз пройти опрос)
```
### Вариант 2: Локально
  можно через докер но мне впдалу было его допилвать
  лучше локально запускайте а то я не тестил че там в докере будет происходить

## 📝 Как это работает?

### Принцип работы:

1. **Ты готовишь вопросы** → вставляешь в `prompt_template.txt`
2. **AI генерирует веса** → отправляешь промпт в ChatGPT/Claude
3. **Сохраняешь конфиг** → вставляешь ответ AI в `survey_answers.md`
4. **Скрипт заполняет форму** → selenium кликает по вариантам с учетом весов

### Как работает генерация ответов:

- Каждый вариант ответа имеет **вес** (0.0 - 1.0)
- Чем больше вес, тем чаще выбирается этот вариант
- AI создает логичное распределение (например, возраст 25-34 чаще, чем 45+)

Пример:
```
Мужской: 0.60  ← выбирается в 60% случаев
Женский: 0.40  ← выбирается в 40% случаев
```

## 🛠️ Пошаговая инструкция

### Шаг 1: Подготовь промпт для AI

1. Открой `prompt_template.txt`
2. Найди секцию `## ВОПРОСЫ ДЛЯ ОБРАБОТКИ:`
3. Вставь туда свои вопросы в таком формате:

```
1. Ваш пол:
Мужской
Женский

2. Ваш возраст:
18–24
25–34
35–44
45 и старше
```

### Шаг 2: Получи веса от AI

1. Скопируй **весь** текст из `prompt_template.txt`
2. Отправь в ChatGPT/Claude/любой AI агент
3. AI вернет что-то такое:

```
### Вопрос 1: Ваш пол
Тип: radio

Ответы:
- Мужской: 0.60
- Женский: 0.40

### Вопрос 2: Ваш возраст
Тип: radio

Ответы:
- 18–24: 0.50
- 25–34: 0.30
- 35–44: 0.15
- 45 и старше: 0.05
```

### Шаг 3: Создай конфигурацию

1. Открой `survey_answers.md`
3. Вставь ответ от AI (из Шага 2)
4. **Важно:** Убедись что текст ответов **ТОЧНО** совпадает с текстом в Google Form!

### Шаг 4: Запусти скрипт

```bash
# Локально
python main.py --url "ССЫЛКА_НА_ТВОЮ_ФОРМУ" -n 50

# Или через Docker
docker compose up
```

## ⚙️ Параметры запуска

```bash
python main.py [OPTIONS]

Обязательные:
  --url TEXT           URL Google Forms

Опциональные:
  -n, --count INT      Количество отправок (по умолчанию: 150)
  --headless           Запуск без окна браузера
  --sleep FLOAT        Задержка между отправками в секундах (по умолчанию: 1.0)
  --config TEXT        Путь к конфигу (по умолчанию: survey_answers.md)
```

### Примеры:

```bash
# 50 отправок с окном браузера
python main.py --url "https://..." -n 50

# 100 отправок в фоновом режиме
python main.py --url "https://..." -n 100 --headless

# С задержкой 2 секунды между отправками
python main.py --url "https://..." -n 50 --sleep 2.0

# С кастомным конфигом
python main.py --url "https://..." --config my_config.md
```

## 📁 Структура проекта

```
auto-answ-gen/
├── main.py                  # Основной скрипт
├── config_parser.py         # Парсер конфигурации
├── prompt_template.txt      # Шаблон промпта для AI
├── survey_answers.md        # Конфигурация ответов (создай сам)
├── requirements.txt         # Зависимости Python
├── docker-compose.yml       # Docker конфигурация
├── Dockerfile              # Docker образ
└── README.md               # Эта инструкция
```

## 🔧 Настройка Docker

Отредактируй `docker-compose.yml`:

```yaml
services:
  sender:
    build: .
    command: ["python", "main.py", "--url", "ТВОЯ_ССЫЛКА", "--count", "150", "--headless", "--sleep", "1"]
    shm_size: "2gb"
```

Или используй переменные окружения (если добавишь их поддержку).

## ⚠️ Важные моменты

### Точность текста

**Критически важно:** Текст ответов в `survey_answers.md` должен **ТОЧНО** совпадать с текстом в Google Form!

- ✅ Правильно: `18–24` (с длинным тире)
- ❌ Неправильно: `18-24` (с обычным дефисом)

### Типы вопросов

- **radio** - один вариант ответа (кружок)
- **checkbox** - несколько вариантов (галочки)

В конфиге указывай правильный тип!

### Порядок вопросов

Вопросы в конфиге должны быть в **том же порядке**, что и в форме. Иначе скрипт может выбрать неправильный вариант.

## 🐛 Решение проблем

### "Config file not found"
→ Создай файл `survey_answers.md` с конфигурацией

### "Failed to select radio/checkbox"
→ Проверь что текст ответов **точно** совпадает с формой
→ Скопируй текст из формы напрямую

### Форма не отправляется
→ Проверь что все обязательные поля заполнены
→ Убедись что тексты ответов правильные

### Ошибки Selenium
→ Убедись что ChromeDriver установлен и актуален
→ Проверь версию Chrome браузера

## 📚 Пример полного workflow

```bash
# 1. Редактирую промпт
vim prompt_template.txt  # Вставляю вопросы

# 2. Отправляю в ChatGPT, получаю ответ
# Копирую ответ в survey_answers.md
vim survey_answers.md

# 3. Запускаю скрипт
python main.py \
  --url "https://docs.google.com/forms/d/e/..." \
  -n 100 \
  --headless \
  --sleep 1.5

# 4. Жду завершения
# Скрипт отправляет 100 ответов с задержкой 1.5 сек
```

## 🤝 Использование с Docker

Если используешь Docker, не забудь:

1. Создать `survey_answers.md` перед запуском
2. Указать URL формы в `docker-compose.yml` или через volumes
3. Проверить что конфиг доступен в контейнере

Пример docker-compose с volume:

```yaml
services:
  sender:
    build: .
    command: ["python", "main.py", "--url", "https://...", "--count", "150", "--headless"]
    shm_size: "2gb"
    volumes:
      - ./survey_answers.md:/app/survey_answers.md
```

## 📝 Формат конфигурации

Каждый вопрос в `survey_answers.md`:

```
### Вопрос N: [Текст вопроса]
Тип: radio

Ответы:
- Вариант 1: 0.XX
- Вариант 2: 0.XX
```

Где:
- `N` - номер вопроса (любой)
- `[Текст вопроса]` - текст из формы (для справки)
- `Тип` - `radio` или `checkbox`
- `Вариант X` - **точный** текст варианта из формы
- `0.XX` - вес от 0.0 до 1.0

## 🔒 Безопасность

- Не коммить `survey_answers.md` (уже в .gitignore)
- Не публикуй реальные URL форм
- Используй разумные задержки между запросами

## 📄 Лицензия

Используй на свой страх и риск. Соблюдай правила использования Google Forms.
